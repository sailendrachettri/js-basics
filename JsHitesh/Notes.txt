-------------------------TO DISPLAY-------------------------
console.log(`
    Full Name is: ${fullName}
    email is: ${email}
    password is: ${password}
    courseCount is : ${courseCount}
`);


------------------------SET PRECISION------------------------
Math.round(discountPercent)