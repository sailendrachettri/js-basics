let str = "Please give Rs 1000";
let amount = console.log(str.slice("Please give Rs ".length) ); // it starts from that length and return from that index to till end

// amount = Number.parseInt(amount);
// amount += 13;
// console.log(amount);